import { moduleFor, test } from 'ember-qunit';

moduleFor('transform:day-of-week', 'Unit | Transform | day of week');

test('it exists', function(assert) {
  const transform = this.subject();
  assert.ok(transform);
});
